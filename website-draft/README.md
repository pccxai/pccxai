# pccx website draft

This directory holds the imported pccx website draft from the local
`/home/hwkim/Desktop/pccx-UI/` source set.

## Source format

The imported draft is static HTML with inline CSS. No framework project markers
were present for Astro, Next, Hugo, or 11ty.

## Integration path

The live homepage is preserved under `public/`, which is the Cloudflare Pages
build output documented by the repository README. This draft is intentionally
isolated under `website-draft/` until it is reviewed and explicitly promoted.

## Imported files

- `index.html` is copied from `preview.html`.
- `assets/logo-wordmark.html` is copied from the final logo source in the same
  local source set.

## Copy guard

Public claims in the draft should remain evidence-gated. Throughput, timing,
bitstream, runtime, and board-level inference claims must stay framed as
targets, planned work, blocked states, or measured results with linked evidence.
